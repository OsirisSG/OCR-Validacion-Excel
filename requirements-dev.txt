# Dependencias de desarrollo, pruebas, tipado y formateo.
-r requirements.txt

# Pruebas unitarias, integración y cobertura
pytest>=8,<10
pytest-cov>=5,<8
httpx>=0.27,<1

# Calidad estática y hooks previos al commit
ruff>=0.5,<1
mypy>=1.10,<2
pre-commit>=3.7,<5
